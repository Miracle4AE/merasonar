version: "3.9"

services:
  maritime-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: maritime-backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
